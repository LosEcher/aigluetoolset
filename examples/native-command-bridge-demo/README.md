# native-command-bridge-demo

Target surface:

- `cantool` frontend bridge wrappers

Purpose:

- validate the headless invoke/subscribe contract
- keep timeout, retry, and normalized error semantics outside product UI code

Run:

```bash
node examples/native-command-bridge-demo/demo.mjs
```
