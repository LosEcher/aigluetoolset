# lsclaw-target-demo

Target surface:

- target-repo onboarding for `lsclaw`

Purpose:

- show the minimal `init -> freeze-slice -> task-run -> verify` loop
- keep all writes inside a temporary repo and clean them automatically

Run:

```bash
node examples/lsclaw-target-demo/run-demo.mjs
```
